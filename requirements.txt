# 3D Gaussian Splatting Reconstruction — 核心可装依赖
#
# 说明：
#   · PyTorch 必须按 CUDA 版本单独安装（勿在本文件指定，见 README「安装」节）：
#       GPU:  pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#       CPU:  pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
#   · OpenCV 必须用 headless 版本（避免与 PySide6 的 Qt 冲突）。
#   · COLMAP（可选，--pose-estimator colmap）需用户自行安装并加入 PATH（见 README）。

numpy>=1.24.0
scipy>=1.10.0
opencv-python-headless>=4.8.0
PySide6>=6.5.0
matplotlib>=3.7.0
psutil>=5.9.0

# 实验性依赖（代码预留接口，默认未启用）
# torchmetrics>=0.11.0
